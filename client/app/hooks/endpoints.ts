/**
 * @fileoverview Re-exports the canonical endpoint factories.
 *
 * Prefer importing directly from `@/lib/endpoints` in new code.
 * This file exists for backwards compatibility with existing hook imports.
 */
export { ENDPOINTS } from "@/lib/endpoints";
export * from "@/lib/endpoints";
